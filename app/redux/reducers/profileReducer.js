let initialState = {
  userName: 'nickname'
}

export default profile = (state = initialState, action) => {
  switch (action.type) {
    case 'TEMP':
      return state;
    default:
      return state;
  }
}
