import React, { useEffect, useState } from 'react';
import {
  Button,
  Dimensions,
  StyleSheet,
  Text,
  TextInput,
  TouchableOpacity,
  TouchableWithoutFeedback,
  View,
  Picker,
} from 'react-native';
import { SafeAreaView } from 'react-native-safe-area-context';

function HelpAndHotlineScreen(props) {

  return (
    <SafeAreaView>
      <Text>Hello World</Text>
    </SafeAreaView>
  )
}

export default HelpAndHotlineScreen;
